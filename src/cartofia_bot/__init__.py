# src/cartofia_bot/commands/__init__.py
"""
Command modules for the Cartofia bot.

Each module in this package should expose a `register(tree, config)` function
that attaches its slash commands to the given CommandTree.
"""
